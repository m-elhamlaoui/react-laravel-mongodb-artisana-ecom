import React from 'react';

const CartPage = ({ items }) => {
    return (
      <div>
        <div
        className="bar"
        style={{ width: "20px" }}
      ></div>
      
      </div>
    );
  };

export default CartPage;
